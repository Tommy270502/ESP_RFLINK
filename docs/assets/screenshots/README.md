# Screenshot Checklist

Add current screenshots for each tagged product release:

- Local web workbench device overview.
- Local web workbench two-dongle validation or support report export.
- Firmware browser dashboard device/RF/log view.
- CLI `wdb report` summary.

Use screenshots in release notes and the project README once the visual state is validated for the release.
